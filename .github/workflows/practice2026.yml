name: Practice 2026 CI

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'practice2026/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0'

      - name: Restore
        run: dotnet restore practice2026/

      - name: Build
        run: dotnet build practice2026/ --no-restore

      - name: Test
        run: dotnet test practice2026/ --no-build

